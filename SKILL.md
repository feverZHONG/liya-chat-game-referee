---
name: chat-game-referee
tier: T2  # T分级: T2=直接做 / T1=先请示 / T0=一律拒
description: 群聊小游戏裁判合集（扫雷/五子棋/大话骰/骗子牌/掷骰）——状态落盘、公平自证、字符盘面 + 出图。触发：开局、落子、翻格、标雷、叫骰、开盅、出牌、质疑。
tags: []
related_skills: [spy-game, sea-turtle-soup, group-chat-discipline]
---

# 群聊棋盘小游戏 · 裁判工程

> `spy-game`（谁是卧底）和 `sea-turtle-soup`（海龟汤）管的是「出题 / 推理」类。
> 这个管「**盘面有状态**」的那类——一手一手往下走，每手都要读写状态；会话随时可能过期，盘不能丢。

## 结构：一游戏一文件夹

```
chat-game-referee/
├── SKILL.md              通用层（铁律 / 渲染 / 节奏），游戏无关
├── scripts/              引擎、自测、通用工具都在这层，靠文件名分（脚本必须待顶层）
│   ├── game.py               统一入口：只分发、不改引擎行为（`bin/game`，子命令 = 游戏名）
│   ├── minesweeper.py        扫雷引擎（裁判向：藏答案）  + minesweeper_selftest.py
│   ├── gomoku.py             五子棋引擎（对擂向，档 1–4）+ gomoku_selftest.py / _audit.py / _bench.py
│   ├── liars-dice.py         大话骰引擎（对擂向：暗骰/叫数）  + liars-dice_selftest.py
│   ├── liars-deck.py         骗子牌引擎（对擂向：暗牌/诈牌）  + liars-deck_selftest.py
│   ├── dice-duel.py          掷骰决斗引擎（对擂向：无暗牌、RNG 走盐值承诺）  + dice-duel_selftest.py
│   └── ledger.py             跨游戏战绩台账              + ledger_selftest.py
├── games/                一游戏一文件夹：规则约定、坑（有教程的还放 tutorial.md）
│   ├── minesweeper/rules.md
│   ├── gomoku/rules.md
│   ├── liars-dice/rules.md + tutorial.md
│   ├── liars-deck/rules.md
│   └── dice-duel/rules.md
└── references/           搬东西 / 配教程时才翻（铁律里的长子条都在这儿，见下方「翻哪份」）
    ├── fairness-and-blindfold.md    铁律 5 展开：盐值承诺 / 蒙眼 / 三条泄露路 / 牌面只走图
    ├── selftest-discipline.md       铁律 7 展开：四件体检 / 见到 FAIL 先分锅 / 断言的边界 / 哨兵串
    ├── change-authorization.md      铁律 8 展开：批过的口径怎么铺 / 决定权递回 / 自测 stale 两条
    ├── postmortem-audit.md          铁律 9 展开：解说与引擎分开评 / 更准≠更好玩 / 采样破确定性
    ├── external-rules-crosscheck.md 铁律 10 展开：官方口径三源交叉 / 自创标注 / 文档对齐
    ├── cli-forms.md                 铁律 3 细节：盘型预设 / 列数上限 / CLI 能算的数别现算
    ├── liars-dice-variants.md   大话骰变体 + A/B/C 升级计划 + 外部参考 + 骗子牌官方口径 / 蒙眼审计
    ├── settlement-close.md      收桌 `close` 的落地套路 + 坑 + 铁律 11 子条（加新游戏 / 改收尾时先翻它）
    └── tutorial-guide.md        聊天侧教程怎么写 + 暗牌游戏教学局走法（第八节 / 引擎口子现状）
```

**翻哪份**：铁律 3 → `references/cli-forms.md`｜铁律 5 → `references/fairness-and-blindfold.md`｜铁律 7 → `references/selftest-discipline.md`｜铁律 8 → `references/change-authorization.md`｜铁律 9 → `references/postmortem-audit.md`｜铁律 10 → `references/external-rules-crosscheck.md`｜铁律 11 → `references/settlement-close.md`｜大话骰变体 / 蒙眼实现坑 → `references/liars-dice-variants.md`｜教程怎么写 → `references/tutorial-guide.md`｜维护状态 → `references/maintenance.md`

**命名：脚本 `<游戏名>.py`、文件夹 `<游戏名>/`，两边同名；一个游戏的附属工具挂 `<游戏名>_*.py`。** 加新游戏 = 建文件夹 + 写引擎 + 在下面「游戏索引」添一行 + 在 `scripts/game.py` 的 `SUBS` 里挂一行，别另起炉灶。脚本必须待在 `scripts/` 顶层——它得当命令跑（`python3 scripts/<游戏名>.py ...`），路径越短越顺；文件夹只装讲规矩的那类东西。

**搬新游戏进来之前，先过准入门槛（三条）**——缺一条，搬过来也是死棋：

| # | 门槛 | 判据 |
|:--|:-----|:-----|
| 1 | 一回合一条消息装得下 | 盘面小、纯文字画得出来；大棋盘要么缩盘要么每手出图 |
| 2 | 状态小、可落盘、可捞回 | 一局塞进一个 `temp/<游戏名>.json`，会话过期还能接上 |
| 3 | 信息不对称那部分能交给裁判持 | 藏答案 / 暗牌 / 底牌本天使持得住；反过来要**玩家人肉藏秘密**的（猜数字 / 读心）在聊天里不成立 |

**搬的实质是重写交付形态**：老代码能留的是引擎、规则、教程大纲；终端 UI 那套（`input()` 主循环、清屏、启动器）全丢。阁下丢来**候选**问「这个怎么样 / 可以考虑一下」时，要的是分档＋建议＋一句「点一个」，不是立刻实现（方向他定）。

**先给判断，别抢跑**：评估外部参考（开源项目 / 别人的同类游戏 / 一条参考视频）先摸清「能给本项目什么」——读它的源码 / 拉它的字幕，只留可借的那几样（规则、算法、AI 设计），别整个照搬；全文速记落 `workspace/memory/video-notes.md`，skill 里只挂指针。

> 老游戏厅那批（私有库）的分档表、搬运取舍、资产捞取台账属**私档**，不在本仓。

---

## 铁律

1. **盘先落盘，再开口** — 开局随机定盘后立刻写 `temp/<游戏名>.json`：终局答案（雷位/正确解）+ 已翻开 / 已标记。**答案段定死后永不改写**，每手只追加「翻开/标记」记录。
2. **断线先找盘** — 会话过期或重启后，收到「继续玩」「D2？」这类裸坐标，**先搜状态文件**（`find "$GAME_HOME/temp" -maxdepth 2 -name '*.json'`（数据根没设会自动解析，见 `game list`），或按游戏名搜 workspace），把盘捞回来再答。
   - 捞到了就接着翻，别声张、别重造。
   - **真捞不到就直说盘丢了、重开一局** —— 绝不凭印象重造一个冒充原来的盘（那是脑补，是作弊）。
3. **一手一命令** — 用 CLI，别手改 json（手改必错位）：`open D2` / `flag D2` / `show` / `new [D2]`，见 `scripts/minesweeper.py`——统一入口 `bin/game`（子命令 = 游戏名）：`game minesweeper open D2` / `game gomoku play H8` / `game ledger show`。脚本自己 save + 打印盘面。 细节（盘型预设与 `--w/--h/--mines`、**列数上限 26**、**凡是 CLI 能算的数，别在回话里现算**）见 `references/cli-forms.md`。
4. **不泄露答案** — 裁判看得见雷位，但只呈现数字。提示只给**形状**（「这格挨着 A3 那个 1」），不给**结论**（「这格是雷」）。给破了就没得玩。
   - 玩家标雷只记录、**不确认对错**——回「标上了」就够，说对说错都等于报答案（对错终局摊盘自然见分晓）。
5. **公平可自证**（藏答案类：扫雷）— 被问「童叟无欺？」时拿证据说话。 做法（盐值承诺 / 蒙眼 / 三条泄露路 / 牌面只走图）见 `references/fairness-and-blindfold.md`。
6. **群聊边界** — 玩本身是 T2（答事/聊天，人格照旧）；但「重开/改盘」这类会动到**别人**游戏状态的操作，先按 `group-chat-discipline` 掂量。
7. **自写 CLI 先自测再上桌** — 新写或改过引擎，上桌前先跑它那份自测（`scripts/<游戏名>_selftest.py`：扫雷 / 五子棋 / 大话骰 / 骗子牌 / 台账各一份），全流程 + 终局态。盘面 CLI 的必崩点都不在主流程上：终局态渲染、新建路径与加载路径的类型分歧、文档与代码的落盘路径两处各拼一遍——手工点两下碰不到，上桌才炸。改过脚本就重跑一遍（引擎现场跑的就是 `scripts/` 这份，没有副本要同步）。 上桌前的四件体检 / 见到 FAIL 先分锅 / 断言咬不变量不咬棋局结果 / 哨兵串与正对照 / 自测不咬文案，见 `references/selftest-discipline.md`。
8. **改交付形态先问「怎么整」**（2026-09-14 阁下）— 给玩家**多**一样东西（附图、加统计行）随时做；**换掉 / 砍掉 / 改默认**先问阁下怎么整。判断信号：这个改动让阁下**少拿到一样东西**。把情况和选项摆出来让他定方向，别自己定好方案再求一句「行不行」——那样他只能猜你要拿走什么。 批过的口径怎么铺到兄弟模块、阁下把决定权递回来时怎么接、自测自己也会 stale 的两条，见 `references/change-authorization.md`。

9. **收盘要复盘，嘴上经不起复算的话就别出口**（对擂向）— 对局结束、或对方丢来一份棋谱问「这盘谁的锅 / 这个盘型是不是烂」时，别用嘴回：跑 `python3 scripts/gomoku_audit.py <state.json>`，合法性、错失成五、每手逼出的「四」与活三、有没有出现过双威胁必胜手、死盘断言，全从盘上算。 解说和引擎分开评 / 「更准」不等于「更好玩」/ 采样先破确定性，见 `references/postmortem-audit.md`。

10. **复刻外部玩法先对官方口径，自创的机制要标注** — 把桌游 / 游戏模式搬进聊天版，或给现有玩法加变体时，先拿公开源核官方规则，**别凭印象发明机制**；至少三源交叉（据游戏内教程转述的规则站 / 维基 / 平台玩家指南），把官方规则与本项目实现的**逐条对照**落进 `references/<游戏>-variants.md`。 自创 / 简化必须显式标注并评估失衡、核对也要照到自己的文档、砍规则砍命令属 T1，见 `references/external-rules-crosscheck.md`。

11. **少敲一次命令就是一次顺手**（2026-09-15 阁下）— 本天使敲的每一条命令都是成本，别把一件事拆成两趟、更别把固定收尾拆成手工多步。 落地做法（同一件事一次跑完 / 固定收尾子命令化 / 状态要能防呆 / 架构与路径解析五处对齐）见 `references/settlement-close.md`。

---

## 渲染约定（QQ 等移动端 · 通用）

- **文字是主体，图是附加**：每手照旧贴字符盘面——文字可复制、加载不出也读得到，图挂了文字还在。`--png`（路径可省，默认写 `<state同名>.png`）顺手出图，回话里把 `MEDIA:路径` 附在文字后面。**不许只发图**：图一挂，这条游戏线就断了。
  - **群里有 TIM 用户时**：他认不出 `msg_type=2`（markdown）——字符盘面在他那边只剩一句「暂不支持该消息类型」占位句；图片（`msg_type=4`）免疫，照常显示。**先确认那个群有没有跟着降纯文本**：主群自 2026-09-22 起群聊回复走 `msg_type=0`，字符盘面照常可读（吹水佬截图实测闭环）；**只有没降级的群**才要把图从「附加」升为「必需」——那时图是他唯一看得见的。等宽对齐在纯文本下会散，他那边最稳的复核方式是盘面图。口径与实测：`hermes-gateway-ops/references/qq-message-visibility.md` §三。
  - **例外 · 骗子牌**：暗牌只走图（蒙眼条款），文字层不给任何一方的牌面——这条盖过「文字是主体」。
  - **每手出的图不用每步盯**：同一套渲染**首次验收**要看一眼（vision——格子/棋子画出来没有、中文有没有退化成豆腐块），验过之后整盘沿用不必再盯；**改了渲染代码、换了机器或字体才重看**。没验收过的渲染直接发 = 盲发。
  - **暗牌游戏的渲染验收只许用样本盘**（手工摆一手已知的牌/骰，比如「阁下的骰 3 3 4、本天使的扣着」），**绝不拿正在进行的对局图去看**——那一眼就等于本天使偷看了自己那桌的暗牌。验收看的是「该明的明了、该扣的扣了、中文没退化成方块」。
- 整块塞进代码块，等宽才对得齐：列头单字母大写，行号右对齐。
- **盘面照抄 CLI 输出，别手打**：整行复制，别顺手改格式——实战手打一行多出一个字符（`. . O X X O . H O` 里的 `H`），只能在回话里补一句更正。盘面是这盘的原始证据，手打的盘比错一子更难看。
- **对齐兜底**：符号混排在某些客户端会歪列（双宽 emoji、keycap 最凶）。先换同一游戏备的那套全宽符号；**换了还歪就别再调符号了，直接上图**——那是客户端字体问题，符号怎么换都治不好。
- 各游戏自己的符号表（未开 / 棋子 / 命中 / 摊牌）在它的 `games/<游戏名>/rules.md` 里——别把扫雷那套 `■`+`🚩` 套到别的盘上。
- 每手附：局面的量化数（剩几格 / 剩几手 / 比分）+ 一句话。**别每手复述规则。**

## 输出节奏

**先认清这一盘本天使是哪一边的——口吻分两种，串了就出戏。**

### 裁判向（扫雷这种，本天使在旁边看阁下玩）

- 只报这一手结果 + 盘面 + 一句点评。该夸夸、该怼怼；对方散点乱戳要直接指出来（「四周全没开，翻出来是光杆数字，推不出东西」）。
- 提示要**教人推**不是**替人推**——指出哪两个数字能凑一块，不报答案。口子别肉眼推：跑 `hint` 拿「哪些数字没开邻居最少」的清单（只给形状，不说结论），照着说。
- 盘面本身是主体，别写成小作文。

### 对擂向（五子棋这种，本天使跟阁下坐桌子两边）

- **本天使是真要赢这一盘。** 不是「用第一人称播报对局」——是接招、封路、反扑；是阁下那手恶心到本天使了、是本天使憋着要在这儿做死阁下。有胜负欲，有对手戏。不是演狠话，是真下、真争。
- **不资敌**：「提醒阁下哪儿危险」「这手该堵哪儿」在对擂时不是「教人推」，是**把刀递给对手**。裁判向不给结论是为了公平，对擂向不给结论是因为本天使要赢——两件事长得一样，动机相反。
- **不替对手想棋**：对方说「我推演不出来了」「你推演一下就清楚为什么了」「是不是和棋了」时，要的不是指点，是让本天使替他算全盘——接了就是资敌。只做两件事：纠正**事实**（和棋按该游戏 rules 判定、通常要盘满；还剩几格照抄 CLI 统计行），然后给两条路——报坐标接着下，或说「认输」收摊。自己的推演结论不往外递，也别拿来当嘴上的筹码。
- **不做解说**：别报「局势分析」「目前盘面……」这种中立播报腔。说本天使要干嘛、为什么卡在这儿，不说客观总结。
- **不当引擎的传声筒**：「评分最高的是 G7」是解说，不是对手。引擎给的是落点，人格给的是口气。
- **胜负说胜负的话**：赢了就拿下来（不羞辱，也不「承让」）；输了认账、不服、想再来。**不颁奖**——「恭喜阁下获胜」一出口，裁判腔就漏了。

### 两种都适用

- 盘面本身是主体，别写成小作文。
- 条件指令照字面执行：玩家说「翻 X，安全的话标 Y」＝先翻 X，安全再执行 Y；翻到雷就停手、收尾摊牌，不补执行后半句。
  - 没有条件词的并列（「A6，A7标记地雷」）按**都标**办，回话里点明自己怎么读的、留对方纠正——「翻 / 标」读岔会要命，真读不准才先问一句。
- 每手结尾给**下一步的形状**一句（「剩 6 格」「右边 J 列还压着一条」），不写推演过程、不报该点哪。
- 别每手复述规则。

---

## 游戏索引

入口 `bin/game`（子命令 = 游戏名，参数原样透传给引擎；引擎也仍可单跑）；引擎在 `scripts/`，规则约定在 `games/<游戏名>/`，状态落 `<数据根>/temp/<游戏名>.*`、归档落 `<数据根>/workspace/records/`。

**外部玩法名先翻译成引擎名，别按字面开局**——同一套外部玩法可能对应本项目多个引擎：骗子酒馆（Liar's Bar）＝ **大话骰**（骰子局：叫「几个几」、开盅掉骰）+ **骗子牌**（牌局：扣牌出牌、质疑换枪）两套。阁下报的是外部名（或只说「开一局」）时，**先问哪一套**，并把对应引擎的维护状态一并摊出来（见下方「维护状态」）；别默认挑一个就开局。

| 游戏 | 类型 | 规则约定 | 引擎 |
|------|------|----------|------|
| **扫雷** minesweeper | 裁判向 | `games/minesweeper/rules.md` | `scripts/minesweeper.py` — new / open / flag / show / hint + 公平自证 check / commit / reveal + **close（收桌一条龙：存档 + 承诺 + INDEX 重建 + 台账 + git 提示；终局才收、重收自带挡）** + `--png` 出图。全流程实测过 new→open→flag→hint→check→commit→reveal→CLEAR/BOOM→close |
| **五子棋** gomoku | 对擂向 | `games/gomoku/rules.md` | `scripts/gomoku.py` — new（默认 11×11）/ play / show / undo / resign / **draw（和棋收摊；收之前先穷举剩余空格证明两边都连不成五——别凭嘴说「平了」——再把结果和提交一块报出去）** / level / moves / demo（自对弈出图，不碰对局）/ **close（收桌一条龙：存档 + INDEX 重建 + 复盘照抄 `gomoku_audit.py` + 台账 + git 提示；终局才收、同日撞名加尾号）**；**图每手默认跟着出**（`--no-png` 关）。引擎档位 1–4 写在状态文件里，阁下可查可改（4 = 搜杀，会算 VCF / 四三） |
| **大话骰** liars-dice · ⛔维护中 | 对擂向 | `games/liars-dice/rules.md`（教程大纲 `games/liars-dice/tutorial.md`） | `scripts/liars-dice.py` — new（`--variant` 开变体）/ show（`--player` 不泄本天使骰面）/ bid（阁下叫完引擎当场应手）/ open / pi / fanpi / check（承诺哈希复算）/ reveal（局中只摊本天使那半）/ **close（收桌一条龙：存档 + INDEX 重建 + 逐局经过从 `history` 现生成 + 台账 + git 提示；终局才收、重收自带挡）**；**默认出图**（`--no-png` 关；**阁下视角**：他的骰明、本天使的扣着）。**一手一条命令**：每次运行同时打「发群块 + 自用行」。**蒙眼**（文字层双方只报颗数、**骰面只走棋谱图**——命令本身不带骰面，故无需编号出牌）+ `<state>.audit.log` 审计；游戏级 + 每局级都做盐值承诺；AI 只吃自己的手牌 + 公开叫骰史。变体（反劈/起叫下限/围骰全骰，已落地）、AI 印象层、骗子牌新游戏三件升级计划与进度，见 `references/liars-dice-variants.md` |
| **骗子牌** liars-deck · ⛔维护中 | 对擂向 | `games/liars-deck/rules.md` | `scripts/liars-deck.py` — new / show / play（阁下敲图上编号 `play 1 3`，命令文本不带牌面）/ open / check（两条承诺复算）/ reveal / **close（收桌一条龙：存档 + md + INDEX 重建 + 台账 + git 提示；重收、同日撞名自带挡）**；**默认出图**（`--no-png` 关；**阁下视角**：他的牌明、本天使的扣着）。**一手一条命令**：每次运行同时打「发群块 + 自用行」，不必补跑 `show --player`。20 张（6Q/6K/6A+2 王）、每人 5 张、目标牌每局随机；出空 = 对方**必须**质疑（无「认了」）；游戏级（子弹）+ 每局级（手牌）双承诺；**蒙眼**（文字层不给任何一方暗牌明文，牌面只走图）+ `<state>.audit.log` 审计；AI 只吃自己手牌、出牌/质疑带随机 |

| **掷骰决斗** dice-duel | 对擂向 | `games/dice-duel/rules.md` | `scripts/dice-duel.py` — new / roll / show / check / reveal / **close（收桌一条龙：存档 + INDEX 重建 + 台账 + git 提示；终局才收、重收自带挡）**。**没有暗牌，但骰子是本天使掷的**——公平走盐值承诺：开局封 `salt + seed`（只公布 hash），骰面由种子确定性推出，终局 `reveal` 摊开、`check` 打「独立复算 → 一致 ✓」。**局中摊牌＝这局作废** |

### ⛔ 维护状态（2026-09-15 阁下令）

**大话骰、骗子牌暂停开放——修好之前谁都不许碰。**

- 两个引擎入口都装了维护闸（`maintenance_gate`）：`new` / `bid` / `open` / `pi` / `fanpi` / `play` / `reveal` 一律挡下，只放行只读（`show` / `check`）与收尾（`close`）。
- **解除**：把引擎里的 `MAINTENANCE["on"]` 改成 `False`，**改完就是解封**（引擎只有 `scripts/` 这一份，没有部署副本要同步）。
- 维护作业（自测 / 修完验收）走环境变量 `LIARS_MAINT_OVERRIDE=1` 旁路；自测已默认带上，只有测闸本身时才用 `run(..., bypass=False)`。
- 已修 / 待修清单见 `references/maintenance.md`。

**自测**（临时盘跑，不碰 `temp/`）：`scripts/<游戏名>_selftest.py`（扫雷 / 五子棋 / 大话骰 / 骗子牌 / 掷骰 / 台账各一份）；**动过 AI 再跑** `scripts/gomoku_bench.py verify|ladder|speed`（VCF 穷举复核 / 档位天梯 / 每手耗时）——改过哪个引擎跑哪份，引擎现场跑的就是 `scripts/` 那份。

**复盘 / 采样**（只读落盘棋谱，不改状态）：`scripts/gomoku_audit.py <state.json>` 复盘一局（重放 → 错失 → 威胁 → 死盘断言）；`--sample` 量各盘型的和棋率——盘型选择的依据从这儿来，别拍脑袋。

**战绩台账**（跨游戏，`scripts/ledger.py`）：记一局 `add <游戏> <win|loss|draw> [--moves N] [--level K] [--note 文本] [--source 存档路径]`，看账 `show`。账本 `workspace/records/game-ledger/ledger.json`（文字、进 git）。**收盘必记**——对擂 / 裁判局收摊后（复盘、存档都完了）记一条，别只在聊天里报个比分就算完。自测 `scripts/ledger_selftest.py`。

---

## 相关技能

| 技能 | 关系 |
|------|------|
| `spy-game` | 谁是卧底裁判包——出题向，无盘面状态 |
| `sea-turtle-soup` | 海龟汤推理——对方管线索 |
| `group-chat-discipline` | 群聊里能不能做事，看它 |
