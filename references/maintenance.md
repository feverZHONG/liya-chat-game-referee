# 维护台账 · 大话骰 / 骗子牌

> 2026-09-15 阁下令：这两个游戏「还是有不少问题」，**纳入维护状态，修好之前谁都不许碰**。
> 引擎入口的维护闸与解除办法见 `SKILL.md` 的「维护状态」小节。

## 一、封存令

- **时间**：2026-09-15
- **范围**：大话骰 `liars-dice`、骗子牌 `liars-deck`（扫雷 / 五子棋 / 台账照常）
- **落地**：两份引擎的 `maintenance_gate` + 本文档 + `SKILL.md` / 两份 `games/liars-*/rules.md` 标注
- **问题面**：阁下后确认「只有这张图」——就是这个泄露，手边没别的清单

## 二、已修

### 1. 结算图把本天使整手暗牌摊给阁下（🔴 信息泄露，2026-09-15）

- **现象**：阁下质疑结算后出的图，把本天使**下一局**的手牌 / 骰面全部画成明面。骗子牌实战漏了一手（图里 5 张全明，牌与状态对得上），阁下当场抓出来。
- **根因**（三件事叠一块）：
  1. 结算函数内部已经推进到新一局——骗子牌 `pump` 重发牌、大话骰 `resolve` 里 `deal(d)` 重摇；
  2. 紧接着 `emit(..., reveal=True)`，而 `render_png` 的 `reveal` 语义是「翻开本天使**整手**」，不是「只翻被质疑的那一手」；
  3. 另一侧路径（本天使质疑阁下）用的是 `reveal=bool(d.get("over"))`——**两边不对称**，所以只在阁下质疑时漏。
- **修复**：三处 `reveal=True` → `reveal=bool(d.get("over"))`（骗子牌 `cmd_open`；大话骰 `_settle` 两处）。
  开盅 / 翻牌的牌面本来就在文字层 `reveal_text` 里（`本天使出的 N 张：…` / `本天使：⚀1 ⚀1 …`），图上不翻**不损失任何公开信息**。
- **取证**（同一局面逐像素对照）：
  - 骗子牌：修前本天使那排是 A/K/Q 正面填充色（238,224,214 / 226,214,196 / 240,228,200），修后全部 `(196,184,166)` 背面灰；阁下那排两张图完全一致。
  - 大话骰：修前黑点像素 2532、修后 0；阁下那排 2376（照旧明着）。
- **防回归**：两份自测各加一条断言 + 一条正对照——骗子牌 `backs()` 判「背面灰张数」、大话骰 `dark_pips()` 判「骰子黑点像素」。
  **判据已用真实泄漏图证伪过**（泄漏图 → 0 张扣着 / 2532 黑点像素，断言会 FAIL）。

## 三、待办

- [x] 「三条泄露路」逐条核（渲染文字层 / 玩家敲进来的命令 / 局中摊牌）——骗子牌与大话骰各核过一遍；
  扫雷（裁判向、藏答案且无对手暗牌）、五子棋（无暗牌）、台账（非游戏）不适用
- [x] 阁下手里的其余问题——已确认没有，就这一条
- [x] ~~骗子牌 `chain` 新一局没清空~~——**不是 bug**：deck 的 `chain` 是「本桌逐局经过」（出牌时追加、跨局累积、`close` 拿它生成存档 md），不是本局出牌链。
  ⚠️ **同名不同义**：大话骰的 `chain` 是「本局叫骰链」（`deal` 时清）。两个引擎各自实现（铁律 11），加减字段前先看清是哪一份。

## 四、活盘（封存时的现场）

| 游戏 | 停在哪 | 状态 |
|------|--------|------|
| 骗子牌 `temp/liars-deck.json` | 第 3 局刚发牌、等阁下先出 | **已脏**：第 3 局本天使的手牌被结算图漏出去过 |
| 大话骰 `temp/liars-dice.json` | 第 2 局、本天使叫了 3 个 4、等阁下表态 | 未结算过，没漏 |

**处置（2026-09-15 阁下批「方案一」）：两盘都原地冻结**——留着当证据，维护完再决定续还是弃。
解封后**别接着旧盘打**，重开新桌。

## 五、解封

- 状态：已修 + 两份自测全绿 + 样本盘验收图已交阁下过目 → **就等阁下一句话**
- 解除动作：引擎 `MAINTENANCE["on"] = False`，且 `temp/` 部署副本同步改回（**两处都改才算解封**）
- 封存时那张泄漏图：`/tmp/evidence-deck.png`（`/tmp` 会被清；长期留档得说一声——图不进 git）
